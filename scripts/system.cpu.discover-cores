#!/bin/bash
#
# Core discovery

echo -n "{\"data\":["

FIRST=1
for core in `cat /proc/cpuinfo | grep ^processor | awk '{print $3}'`; do
	if [ "${FIRST}" = "0" ]; then
		echo -n ","
	fi
	echo -n "{\"{#CORE}\":\"$core\"}"
	FIRST=0
done

echo -n "]}"
echo

exit 0

